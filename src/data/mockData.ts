import { GIVEAWAY_CONFIG } from '../giveawayConfig';
import { ContestPost, Participant } from '../types';

export const CONTEST_POST: ContestPost = GIVEAWAY_CONFIG.post;
export const PARTICIPANTS: Participant[] = GIVEAWAY_CONFIG.allParticipants;
